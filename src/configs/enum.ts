export enum EPages {
    HOME = "/",
    WORK = "/work",
    PROJECT = "/project",
    ABOUT = '/about',
    SERVICES = "/services",
    CONTACT = "/contact",
}
export enum ELanguages {
  EN = "en",
  KM = "km",
}